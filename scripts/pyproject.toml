[project]
name = "Scripts"
version = "0.1.0"
description = "Venv for scripts"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "qrcode-artistic>=3.0.2",
    "qrcode[pil]>=8.2",
    "segno>=1.6.6",
]
